<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="samy.comptecafet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cercle_imag_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".vue.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".vue.EatActivity" />
        <activity android:name=".vue.MoneyActivity" />
        <activity android:name=".vue.HistoryActivity">
        </activity>
    </application>

</manifest>